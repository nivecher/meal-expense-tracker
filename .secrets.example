# Example secrets file for act testing
# Copy this to .secrets and fill in values (or leave empty for local testing)
# Note: Some secrets won't work locally (AWS_ROLE_ARN, GITHUB_TOKEN, etc.)

# AWS Configuration (optional for local testing)
AWS_ROLE_ARN=arn:aws:iam::123456789012:role/GitHubActions
AWS_REGION=us-east-1

# GitHub Token (optional - won't work for real GHCR access locally)
GITHUB_TOKEN=ghp_example_token_here

# Other secrets as needed
# Add any other secrets your workflow requires
