service: meal-expense-tracker

provider:
  name: aws
  runtime: python3.13
  stage: dev
  region: us-east-1

functions:
  hello:
    handler: handler.hello
    events:
      - http:
          path: /
          method: get
