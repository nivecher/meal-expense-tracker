[general]
ignore=trailing-whitespace, body-first-line-empty, body-max-line-length, title-max-line-length

[rule "title-match-regex"]
regex = ^([a-z]+)\([a-z0-9_\-]+\): .+
message = Title must match format: <type>(<scope>): <description>

[rule "title-types"]
valid_types = feat, fix, docs, style, refactor, perf, test, chore, ci, build
